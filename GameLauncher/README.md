@"
# GameLauncher

โปรเจกต์ PowerShell loader สำหรับเกม

## วิธีใช้
1. แก้ไขไฟล์ `scripts/load-game.ps1` ให้ `$ConfigUrl` เป็น URL ของ JSON config บน GitHub
2. เปิด PowerShell
3. รันสคริปต์:
   ```powershell
   cd C:\Users\Administrator\Desktop\GameLauncher\scripts
   .\load-game.ps1
